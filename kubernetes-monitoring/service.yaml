apiVersion: v1
kind: Service
metadata:
  name: custom-nginx
  namespace: default
spec:
  selector:
    app: custom-nginx
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP