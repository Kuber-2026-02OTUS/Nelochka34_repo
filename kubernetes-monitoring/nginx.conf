events {}

http {
    server {
        listen 8080;

        location / {
            return 200 'Hello from custom nginx\n';
        }

        location /metrics {
            stub_status;
            allow all;
        }
    }
}