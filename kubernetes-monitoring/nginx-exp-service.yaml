apiVersion: v1
kind: Service
metadata:
  name: nginx-exporter
  namespace: default
spec:
  selector:
    app: nginx-exporter
  ports:
    - name: http
      port: 9113
      targetPort: 9113
  type: ClusterIP