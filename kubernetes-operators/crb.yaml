apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: mysql-full-access-binding

subjects:
  - kind: ServiceAccount
    name: mysql-sa
    namespace: default

roleRef:
  kind: ClusterRole
  name: mysql-full-access
  apiGroup: rbac.authorization.k8s.io