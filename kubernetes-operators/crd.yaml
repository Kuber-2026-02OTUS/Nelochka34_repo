apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: mysql.otus.homework

spec:
  group: otus.homework

  scope: Namespaced

  names:
    plural: mysql
    singular: mysql
    kind: MySQL
    shortNames:
      - my

  versions:
    - name: v1
      served: true
      storage: true

      schema:
        openAPIV3Schema:
          type: object