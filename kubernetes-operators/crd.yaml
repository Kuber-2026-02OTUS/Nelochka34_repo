apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition

metadata:
  name: mysqls.otus.homework

spec:
  group: otus.homework

  scope: Namespaced

  names:
    plural: mysqls
    singular: mysql
    kind: MySQL

  versions:
    - name: v1
      storage: true

      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object